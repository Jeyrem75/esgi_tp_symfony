{% extends 'base.html.twig' %}

{% block body %}
<div class="container mt-4">
    <h1 class="mb-4 text-center">Dashboard Administrateur</h1>
    <div class="row text-center">
        <div class="col-md-4 mb-3">
            <a href="{{ path('app_user_index') }}" class="btn btn-primary btn-lg w-100">
                Gérer les Utilisateurs
            </a>
        </div>
        <div class="col-md-4 mb-3">
            <a href="{{ path('app_recipe_index') }}" class="btn btn-success btn-lg w-100">
                Gérer les Recettes
            </a>
        </div>
        <div class="col-md-4 mb-3">
            <a href="{{ path('app_ingredient_index') }}" class="btn btn-info btn-lg w-100">
                Gérer les Ingrédients
            </a>
        </div>
        <div class="col-md-4 mb-3">
            <a href="{{ path('app_step_index') }}" class="btn btn-warning btn-lg w-100">
                Gérer les Étapes
            </a>
        </div>
        <div class="col-md-4 mb-3">
            <a href="{{ path('app_review_index') }}" class="btn btn-danger btn-lg w-100">
                Gérer les Avis
            </a>
        </div>
    </div>
</div>
{% endblock %}
