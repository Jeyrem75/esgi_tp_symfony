{% extends 'base.html.twig' %}

{% block title %}Modifier l'Ingrédient{% endblock %}

{% block body %}
<div class="container mt-4">
    <h1 class="text-center mb-4">Modifier l'Ingrédient</h1>

    {{ include('ingredient/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

    <div class="mt-4 d-flex justify-content-between">
        <a href="{{ path('app_ingredient_index') }}" class="btn btn-secondary">
            <i class="fas fa-arrow-left"></i> Retour à la liste
        </a>
        {{ include('ingredient/_delete_form.html.twig') }}
    </div>
</div>
{% endblock %}
