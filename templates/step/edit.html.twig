{% extends 'base.html.twig' %}

{% block title %}Modifier l'Étape{% endblock %}

{% block body %}
<div class="container mt-4">
    <h1 class="text-center mb-4">Modifier l'Étape</h1>

    {{ include('step/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

    <div class="mt-4 d-flex justify-content-between">
        <a href="{{ path('app_step_index') }}" class="btn btn-secondary">
            <i class="fas fa-arrow-left"></i> Retour à la liste
        </a>
        <div>
            {{ include('step/_delete_form.html.twig') }}
        </div>
    </div>
</div>
{% endblock %}
